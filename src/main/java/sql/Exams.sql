CREATE TABLE EXAMS (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NAME VARCHAR2(100) NOT NULL,
    EXAMDATE VARCHAR2(100),
    CREDIT VARCHAR2(100),
    SCORE VARCHAR2(100),
    CREATED_AT DATE DEFAULT SYSDATE
);

INSERT INTO EXAMS (NAME, EXAMDATE, CREDIT, SCORE) VALUES ('XDIAK', '11.06.2025', '4', '66/100');
COMMIT;